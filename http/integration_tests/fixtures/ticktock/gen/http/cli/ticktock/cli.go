// Code generated by Loom, DO NOT EDIT.
//
// ticktock HTTP client CLI support package
//
// Command:
// $ loom gen example.com/http-ticktock/design -o .

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	clockc "example.com/http-ticktock/gen/http/clock/client"
	loomhttp "github.com/CaliLuke/loom/http"
	loom "github.com/CaliLuke/loom/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{"clock (tick|tock|guarded)"}
} // UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " clock tick\\n"
} // ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(scheme string, host string, doer loomhttp.Doer, enc func(*http.Request) loomhttp.Encoder, dec func(*http.Response) loomhttp.Decoder, restore bool) (loom.Endpoint, any, error) {
	var (
		clockFlags            = flag.NewFlagSet("clock", flag.ContinueOnError)
		clockTickFlags        = flag.NewFlagSet("tick", flag.ExitOnError)
		clockTockFlags        = flag.NewFlagSet("tock", flag.ExitOnError)
		clockGuardedFlags     = flag.NewFlagSet("guarded", flag.ExitOnError)
		clockGuardedTokenFlag = clockGuardedFlags.String("token", "", "")
	)
	clockFlags.Usage = clockUsage
	clockTickFlags.Usage = clockTickUsage
	clockTockFlags.Usage = clockTockUsage
	clockGuardedFlags.Usage = clockGuardedUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}
	if flag.NArg() < 2 {
		return nil, nil, fmt.Errorf("not enough arguments")
	}
	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "clock":
			svcf = clockFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}
	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "clock":
			switch epn {
			case "tick":
				epf = clockTickFlags
			case "tock":
				epf = clockTockFlags
			case "guarded":
				epf = clockGuardedFlags
			}
		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint loom.Endpoint
		err      error
	)
	{
		switch svcn {
		case "clock":
			c := clockc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "tick":
				endpoint = c.Tick()
			case "tock":
				endpoint = c.Tock()
			case "guarded":
				endpoint = c.Guarded()
				data, err = clockc.BuildGuardedPayload(*clockGuardedTokenFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}
	return endpoint, data, nil
} // clockUsage displays the usage of the clock command and its subcommands.
func clockUsage() {
	fmt.Fprintln(os.Stderr, "Service is the clock service interface.")
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] clock COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, "    tick: Tick implements Tick.")
	fmt.Fprintln(os.Stderr, "    tock: Tock implements Tock.")
	fmt.Fprintln(os.Stderr, "    guarded: Guarded implements Guarded.")
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s clock COMMAND --help\n", os.Args[0])
}
func clockTickUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] clock tick", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Tick implements Tick.")

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "clock tick")
}
func clockTockUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] clock tock", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Tock implements Tock.")

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "clock tock")
}
func clockGuardedUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] clock guarded", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Guarded implements Guarded.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -token STRING: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "clock guarded --token \"Incidunt facere officiis est et est tempore.\"")
}
