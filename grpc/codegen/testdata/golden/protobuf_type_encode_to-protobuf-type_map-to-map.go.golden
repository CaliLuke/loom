func transform() {
	target := &proto.SimpleMap{}
	if source.Simple != nil {
		target.Simple = make(map[string]int64, len(source.Simple))
		for key, val := range source.Simple {
			tv := int64(val)
			target.Simple[key] = tv
		}
	}
}
