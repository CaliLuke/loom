// Code generated by goa, DO NOT EDIT.
//
// ticktock JSON-RPC client CLI support package
//
// Command:
// $ goa gen example.com/ticktock/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	clockc "example.com/ticktock/gen/jsonrpc/clock/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"clock (tick|tock)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "clock tick --body '{\n      \"id\": \"Aut delectus.\"\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		clockFlags = flag.NewFlagSet("clock", flag.ContinueOnError)

		clockTickFlags    = flag.NewFlagSet("tick", flag.ExitOnError)
		clockTickBodyFlag = clockTickFlags.String("body", "REQUIRED", "")

		clockTockFlags    = flag.NewFlagSet("tock", flag.ExitOnError)
		clockTockBodyFlag = clockTockFlags.String("body", "REQUIRED", "")
	)
	clockFlags.Usage = clockUsage
	clockTickFlags.Usage = clockTickUsage
	clockTockFlags.Usage = clockTockUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "clock":
			svcf = clockFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "clock":
			switch epn {
			case "tick":
				epf = clockTickFlags

			case "tock":
				epf = clockTockFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "clock":
			c := clockc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "tick":
				endpoint = c.Tick()
				data, err = clockc.BuildTickPayload(*clockTickBodyFlag)
			case "tock":
				endpoint = c.Tock()
				data, err = clockc.BuildTockPayload(*clockTockBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// clockUsage displays the usage of the clock command and its subcommands.
func clockUsage() {
	fmt.Fprintln(os.Stderr, `Service is the clock service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] clock COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    tick: Tick implements Tick.`)
	fmt.Fprintln(os.Stderr, `    tock: Tock implements Tock.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s clock COMMAND --help\n", os.Args[0])
}
func clockTickUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] clock tick", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Tick implements Tick.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "clock tick --body '{\n      \"id\": \"Aut delectus.\"\n   }'")
}

func clockTockUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] clock tock", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Tock implements Tock.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "clock tock --body '{\n      \"id\": \"Voluptas alias nihil consectetur qui a voluptatem.\"\n   }'")
}
