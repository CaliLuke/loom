\* The fix of #481 as shipped: the map first, rollback on a failed group
\* creation, and RemoveStream as on main (separate map update and group
\* destroy). Expect NoStaleMember to hold. AddedHasGroup is not checked
\* here: the RemoveStream race (#508) violates it, see
\* fixed_remove_asis.cfg.
SPECIFICATION Spec
CONSTANTS
    Instances = {i1, i2, i3}
    CREATE_FIRST = FALSE
    ROLLBACK = TRUE
    ATOMIC_REMOVE = FALSE
INVARIANTS
    TypeOK
    NoStaleMember
