\* The fix of #481 with RemoveStream before the fix of #508: the map update
\* and the unconditional group destroy are separate calls. Expect an
\* AddedHasGroup violation that the fix of #481 does not cause and does not
\* fix (issue #508). NewSink is not modeled.
SPECIFICATION Spec
CONSTANTS
    Instances = {i1, i2}
    CREATE_FIRST = FALSE
    ROLLBACK = TRUE
    REMOVE = "split"
    NEW_SINK = "none"
INVARIANTS
    TypeOK
    NoStaleMember
    AddedHasGroup
