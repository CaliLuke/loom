\* The fix of #481 with RemoveStream as on main: the map update and the
\* group destroy are separate calls. Expect an AddedHasGroup violation that
\* the fix of #481 does not cause and does not fix (issue #508).
SPECIFICATION Spec
CONSTANTS
    Instances = {i1, i2}
    CREATE_FIRST = FALSE
    ROLLBACK = TRUE
    ATOMIC_REMOVE = FALSE
INVARIANTS
    TypeOK
    NoStaleMember
    AddedHasGroup
