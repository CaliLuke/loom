\* Main before the fix of #481: the map first, no rollback. Expect a
\* NoStaleMember violation. RemoveStream is the fix of #508. NewSink is not
\* modeled.
SPECIFICATION Spec
CONSTANTS
    Instances = {i1, i2}
    CREATE_FIRST = FALSE
    ROLLBACK = FALSE
    REMOVE = "checked"
    NEW_SINK = "none"
INVARIANTS
    TypeOK
    NoStaleMember
