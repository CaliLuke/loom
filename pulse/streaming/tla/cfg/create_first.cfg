\* The alternative fix of #481: the group first, then the map. Expect an
\* AddedHasGroup violation: a concurrent RemoveStream destroys the group
\* between the two steps, even with the checked destroy of #508.
SPECIFICATION Spec
CONSTANTS
    Instances = {i1, i2}
    CREATE_FIRST = TRUE
    ROLLBACK = FALSE
    REMOVE = "checked"
INVARIANTS
    TypeOK
    NoStaleMember
    AddedHasGroup
