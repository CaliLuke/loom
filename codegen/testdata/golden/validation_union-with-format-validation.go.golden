func Validate() (err error) {
	switch string(target.Response.Kind()) {
	case "timestamp":
		actual, _ := target.Response.AsTimestamp()
		err = loom.MergeErrors(err, loom.ValidateFormat("target.response.value", string(actual), loom.FormatDateTime))
	}
}
